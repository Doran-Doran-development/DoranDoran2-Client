import { RoomDetail } from "components";
import React from "react";

interface Props {}

const RoomDetailContainer = (props: Props) => {
  return <RoomDetail />;
};

export default RoomDetailContainer;
